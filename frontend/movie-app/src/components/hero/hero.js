import './Hero.css';
import Carousel from 'react-material-ui-carousel';
import { Paper } from '@mui/material';



const hero = () => {
  return (
    <div>
        <Carousel>
            movies.map({movie} =>{

            })
            </Carousel>
    </div>
  )
}

export default hero